;You can place description/hints for specific sub_page/page/module here
;They will be shown automatically at the top of the page
;Available keywords: see table 'modules', field 'module_name'

[file_bulk]
在此处添加*本地文件（即，服务器上已经存在的文件）。<br>

请为所有要添加的文件建立一些目录。

将应用拒绝或允许扩展名规则（参见“设置”页签）。

不会复制或移动文件。<br><br>
* 以这种方式添加文件会在数据库中添加一条含文件路径的记录。请不要在添加后移动或删除服务器的文件。

[file_rule_insert]
计划任务将对指定目录进行分析，并将找到的文件添加/更新到知识库<br>

将应用拒绝或允许扩展名规则（参见“设置”页签）。

不会复制或移动文件。<br><br>
* 以这种方式添加文件会在数据库中添加一条含文件路径的记录。

[read_dir]
已应用拒绝或允许扩展名规则（参见“设置”页签）。